hi this is a tiny music player using *[raylib](https://github.com/raysan5/raylib)*.

# how use
to use upload *music files* preferably in `.ogg` format into a `music/` directory.
every music file is loaded upon launch

# how build
run `./build_win` to build for windows.
no linux.